version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: olhari_postgres
    restart: always
    environment:
      POSTGRES_DB: olhari
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"